# Company logo source for the "Companies I've worked with" section.
#   runtime -> fetched from a logo service by company domain at view time (default;
#              self-hosted SVG monogram is used as the fallback)
#   hosted  -> always use the self-hosted SVGs in /public/companies/<slug>.svg
PUBLIC_COMPANY_LOGO_SOURCE=runtime

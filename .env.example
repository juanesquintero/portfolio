# Company logo source for the "Companies I've worked with" section.
#   hosted  -> self-hosted SVGs in /public/companies/<slug>.svg (default)
#   runtime -> fetched from a logo service by company domain at view time
#              (self-hosted SVG is used as the fallback)
PUBLIC_COMPANY_LOGO_SOURCE=hosted
